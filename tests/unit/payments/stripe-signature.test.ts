// Stripe has been removed from this project. This test file is kept as a
// placeholder so the test runner doesn't error on missing files.
// See tests/unit/payments/razorpay-signature.test.ts for payment tests.
import { describe, it, expect } from 'vitest';

describe('Stripe (removed)', () => {
  it('Stripe has been removed — Razorpay is the sole payment gateway', () => {
    expect(true).toBe(true);
  });
});
